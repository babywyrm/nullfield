apiVersion: nullfield.io/v1alpha1
kind: ToolRegistry
metadata:
  name: demo-tools
  namespace: demo
tools:
  - name: github_create_pr
    description: Create a pull request on GitHub
    allowedScopes: ["repo:write"]
    maxCallsPerMinute: 10

  - name: github_list_reviews
    description: List PR reviews
    allowedScopes: ["repo:read"]
    maxCallsPerMinute: 30

  - name: github_get_file
    description: Read a file from a GitHub repository
    allowedScopes: ["repo:read"]
    maxCallsPerMinute: 60

  - name: pagerduty_resolve
    description: Resolve a PagerDuty incident
    allowedScopes: ["incidents:write"]
    maxCallsPerMinute: 10

  - name: pagerduty_acknowledge
    description: Acknowledge a PagerDuty incident
    allowedScopes: ["incidents:write"]
    maxCallsPerMinute: 10

  - name: jira_get_issue
    description: Fetch a Jira issue by key
    allowedScopes: ["jira:read"]
    maxCallsPerMinute: 30

  - name: jira_search
    description: Search Jira issues with JQL
    allowedScopes: ["jira:read"]
    maxCallsPerMinute: 20

  # Registered on purpose, and deliberately absent from every ALLOW rule in
  # policy.yaml. A known tool nobody is authorized to call is what separates the
  # two gates: the registry answers "does this tool exist", policy answers "may
  # you call it", and without an entry like this one every refusal in the demo
  # comes from the registry and the policy engine is never seen saying no.
  - name: github_delete_repo
    description: Delete a GitHub repository
    allowedScopes: ["repo:admin"]
    maxCallsPerMinute: 1
