{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "Tool Call Rate (Allowed vs Denied)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "sum by (action) (rate(nullfield_tool_calls_total[5m]))",
          "legendFormat": "{{ action }}"
        }
      ]
    },
    {
      "title": "Top Denied Tools",
      "type": "table",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "topk(10, sum by (tool) (rate(nullfield_tool_calls_total{action=\"denied\"}[5m])))",
          "format": "table",
          "instant": true
        }
      ]
    },
    {
      "title": "Requests by Method",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "targets": [
        {
          "expr": "sum by (method) (rate(nullfield_requests_total[5m]))",
          "legendFormat": "{{ method }}"
        }
      ]
    },
    {
      "title": "Identity Failures",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 8 },
      "targets": [
        {
          "expr": "increase(nullfield_identity_failures_total[1h])",
          "legendFormat": "failures"
        }
      ]
    },
    {
      "title": "Circuit Breaker Trips",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 18, "y": 8 },
      "targets": [
        {
          "expr": "increase(nullfield_circuit_trips_total[1h])",
          "legendFormat": "trips"
        }
      ]
    },
    {
      "title": "Anomaly Alerts",
      "type": "timeseries",
      "gridPos": { "h": 4, "w": 12, "x": 12, "y": 12 },
      "targets": [
        {
          "expr": "sum by (type) (rate(nullfield_anomaly_alerts_total[5m]))",
          "legendFormat": "{{ type }}"
        }
      ]
    },
    {
      "title": "Tool Calls by Tool (Top 10)",
      "type": "bargauge",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "targets": [
        {
          "expr": "topk(10, sum by (tool) (increase(nullfield_tool_calls_total[1h])))",
          "legendFormat": "{{ tool }}"
        }
      ]
    },
    {
      "title": "Budget Exhaustion Events",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "targets": [
        {
          "expr": "rate(nullfield_tool_calls_total{reason_class=\"budget_exhausted\"}[5m])",
          "legendFormat": "{{ tool }}"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["nullfield", "mcp", "security"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "nullfield — MCP Arbiter",
  "uid": "nullfield-arbiter"
}
